# Large dependencies - downloaded by setup scripts
app/core/node_modules/
app/runtime/
app/core/package-lock.json
# Generated by setup.{sh,bat,ps1} on every run from OPENCLAW_VERSION.
# Tracking it caused dirty git status post-setup and risked drift
# between hard-coded deps and what setup actually generates. (B22-04)
app/core/package.json

# User data
data/

# OS files
.DS_Store
.localized

# Build artifacts
*.zip
*.tar.gz
*.dmg
*.exe

# IDE
.vscode/
.idea/
